#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i, sum;

	for (i = 1; a1 < 1024; a1++)
	{
		if ((i * 3) < 1024)
		{
			sum += i * 3;
		} else if ((i * 5) < 1024)
		{
			sum += i * 5;
		} else
		{
			break;
		}
	}
	return (0);
}
