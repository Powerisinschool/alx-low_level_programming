#include <math.h>
#include "main.h"

/**
 * _abs - Entry point
 * @n: 'rtfm abs'
 *
 * Description: 'the program's description'
 *
 * Return: Absolute value of input number
 */

int _abs(int n)
{
	return (pow(pow(n, 2), 0.5));
}
