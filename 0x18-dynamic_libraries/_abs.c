#include "main.h"

/**
 * _abs - Entry point
 * @n: 'rtfm abs'
 *
 * Description: 'the program's description'
 *
 * Return: Absolute value of input number
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (n);
}
